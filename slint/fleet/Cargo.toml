[package]
name = "atlas-fleet"
version = "0.2.0"
edition = "2024"
rust-version = "1.92"
publish = false
license = "MIT"

[dependencies]
atlas-ui = { path = "../../../Atlas/crates/atlas-ui", version = "=0.2.0" }
image = { version = "=0.25.10", default-features = false, features = ["png"] }
slint = "=1.18.0"

[build-dependencies]
atlas-ui = { path = "../../../Atlas/crates/atlas-ui", version = "=0.2.0" }
slint-build = "=1.18.0"

[workspace]
